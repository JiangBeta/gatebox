# ADR-010：MVP 优先

- 状态：已接受
- 日期：2026-08-20

## 背景

控制面初期规模应最小化，快速跑通闭环。

## 决策

**MVP 优先**：认证采用单用户 admin（JWT）；多用户、RBAC、UDP 代理、独立证书签发、多宿主机、发行版原生包等**后置**。

## 后果

- 优点：快速交付最小可用模型。
- 约束：数据模型与接口预留扩展空间（如认证体系、代理类型枚举）。
