# ADR-004：DDNS 依赖外部 ddnsgo

- 状态：已接受
- 日期：2026-08-20

## 背景

「二级域名 DNS 自动化」需要周期性把域名解析到本机动态公网 IP。

## 决策

依赖**外部 ddnsgo**（而非自研内嵌 DDNS 上报）。控制面通过**改写 ddnsgo 的 YAML 配置 + 重启服务**来全自动完成上报（ddnsgo 无 REST API，配置为 YAML 文件，改后需重启生效）。

## 后果

- 优点：复用成熟工具、避免自研多提供商适配。
- 约束：需实现 ddnsgo YAML 的读写与「改后重启」的触发逻辑。
