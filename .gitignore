# 前端
node_modules/
components.d.ts

# 前端构建产物（内嵌到后端 embed，仅保留占位以通过编译）
**/internal/web/dist/*
!**/internal/web/dist/.gitkeep

# 后端构建产物
/gatebox
/backend/gatebox
/old/backend/gatebox

# 运行时数据
data/
backend/data/
*.db

# 日志与备份文件
*.log
*.bak

# 注:仓库 tools/ 存放外部组件二进制并入库(ADR-011 修订,2026-09-07),勿再屏蔽。
